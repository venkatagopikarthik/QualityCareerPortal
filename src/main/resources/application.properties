spring.application.name=demoTask

# Use dynamic port for Railway (fallback to 8080 if PORT not found)
server.port=${PORT:8080}

# View Configuration (JSP)
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# MySQL (replace these values with Railway MySQL plugin details)
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=UFlTJFWYOLjWOYHvgXEBETwjAtLurJYv
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# Disable H2 (you’re using MySQL now)
spring.h2.console.enabled=false

# Spring Security (Optional, only if you're using it for dev access)
spring.security.user.name=Tharun@123
spring.security.user.password=Mahi@123
spring.security.user.roles=ADMIN
